import * as index from '../src';

/**
 * Quick test to make sure we're not regressing on user
 * accessible functions / classes.
 */
test('index', () => {
  expect(Object.keys(index)).toMatchSnapshot();
});
